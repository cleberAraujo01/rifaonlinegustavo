# URL do banco Neon Postgres (Vercel Marketplace ou neon.tech)
DATABASE_URL="postgresql://usuario:senha@host/db?sslmode=require"

# Hash bcrypt da senha do admin (gerar com: npm run admin:hash -- "sua-senha")
ADMIN_PASSWORD_HASH=""

# Segredo para assinar o cookie de sessão (gerar com: openssl rand -base64 32)
SESSION_SECRET=""
